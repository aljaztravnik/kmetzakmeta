@model IEnumerable<web.Models.KategorijaStroja>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MachineType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MachineType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MachineTypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MachineTypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MachineTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
